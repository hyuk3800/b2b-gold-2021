extends ../base2

block content 
       main 
            if errorMessage
                span=errorMessage
    
            form(method="POST") 
                input(name="username", placeholder="ID", required, type="text")
                input(name="password", placeholder="Password", required, type="password")
                input(type="submit" value="로그인")